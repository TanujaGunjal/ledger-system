# Testcontainers on Windows Docker Desktop requires the TCP socket to be enabled.
# In Docker Desktop → Settings → General → check:
#   "Expose daemon on tcp://localhost:2375 without TLS"
# Then restart Docker Desktop. This file tells Testcontainers to use that socket.
docker.host=tcp://localhost:2375
